services:
  # Base de Datos PostgreSQL
  - type: database
    name: erp-db-a37b
    databaseName: erp_8amperios
    user: erp_admin
    plan: free

  # Backend - Django Web Service
  - type: web
    name: erp-backend-a37b
    runtime: python
    plan: free
    buildCommand: "chmod +x build.sh && ./build.sh"
    startCommand: "python manage.py migrate --no-input && gunicorn erp_core.wsgi:application --workers 2 --timeout 120"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: erp-db-a37b
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "erp-backend-a37b.onrender.com,.onrender.com,localhost,127.0.0.1"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "False"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://erp-frontend-7798.onrender.com,https://erp-backend-a37b.onrender.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://erp-frontend-7798.onrender.com,https://erp-backend-a37b.onrender.com,https://*.onrender.com"

  # Frontend - React Static Site
  - type: static
    name: erp-frontend-7798
    runtime: static
    buildCommand: "cd frontend && npm install && npm run build"
    staticPublishPath: "frontend/dist"
    envVars:
      - key: VITE_API_URL
        value: https://erp-backend-a37b.onrender.com/api
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
